type User {
    id: ID!
    username: String!
    email: String!
    nickname: String
    profile_color: String
}

type TokenMessage {
    token: String
    message: String!
    user: User!
}

input Credentials {
    email: String!
    password: String!
}

input UserInput {
    username: String!
    email: String!
    password: String!
}

input UserModify {
    username: String
    nickname: String
    email: String
    profile_color: String
    password: String
}

type Query {
    users: [User]
}

type Mutation {
    register(user: UserInput!): TokenMessage
    login(credentials: Credentials!): TokenMessage
}
