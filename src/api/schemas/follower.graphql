type Follow {
    user: ID!
    target: User!
}

type Query {
    followers: [Follow]
    following: [Follow]
    followMutuals: [ID]
}

type Mutation {
    followAdd(target_id: ID!): Follow
    followRemove(target_id: ID!): Follow
}
